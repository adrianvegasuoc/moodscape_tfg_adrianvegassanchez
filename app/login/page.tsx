import { SectionPlaceholder } from "@/components/section-placeholder";

export default function LoginPage() {
  return (
    <SectionPlaceholder
      title="Login"
      description="Pantalla inicial reservada para el acceso de usuarios."
    />
  );
}
